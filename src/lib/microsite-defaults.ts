import type { MicrositeQuickAction } from "@/types/microsite";

export const DEFAULT_MICROSITE_ACCENT = "#0f7eff";
export const DEFAULT_MICROSITE_BUTTON = "#0f7eff";

/** Genérico a propósito: Mi Link lo usan negocios de cualquier rubro, no solo aseguradoras. */
export const DEFAULT_MICROSITE_GREETING = "Estoy aquí para ayudarte. Escríbeme lo que necesites.";

export const DEFAULT_MICROSITE_QUICK_ACTIONS: MicrositeQuickAction[] = [];
